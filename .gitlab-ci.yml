stages:
  - release

release:
  tags:
    - build-shell
  stage: release
  image:
    name: goreleaser/goreleaser
    entrypoint: ['']
  only:
    - tags
  variables:    
    GITLAB_TOKEN: $CI_JOB_TOKEN
    GIT_DEPTH: 0
  script:
    - goreleaser release --clean